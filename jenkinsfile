pipeline {
    agent any

    stages {
        stage('Check SonarScanner Installation') {
            steps {
                sh 'sonar-scanner -v'  // Check if SonarScanner is installed and display the version
            }
        }
    }

    post {
        always {
            echo "SonarScanner verification completed."
        }
    }
}
