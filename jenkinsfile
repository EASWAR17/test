pipeline {
    agent any
    environment {
        PATH = "/opt/sonar-scanner/bin:${env.PATH}"
    }
    stages {
        stage('Check SonarScanner Installation') {
            steps {
                sh 'sonar-scanner -v'
            }
        }
        // Other stages...
    }
}
